// Reasons the game would be/is paused.
public enum GamePauseReason
{
    NotPaused, // Game is running.
    TutorialHint, // Paused for tutorial hint.
    MenuOpen, // Paused because a menu is open (Pause/Settings).
}
